{
  "name": "thebadge",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:dev": "yarn build && graph deploy --product hosted-service thebadgeadmin/dev-testing",
    "deploy:studio": "graph deploy --studio thebadge",
    "auth": "./scripts/auth.sh"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.49.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}
