{
  "name": "thebadge",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:dev": "yarn build && graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ thebadgeadmin/dev-testing",
    "deploy:studio": "graph deploy --studio thebadge",
    "auth": "graph auth --product hosted-service eval $(grep THE_GRAPH_DEV_TESTING_AUTH_TOKEN .env | cut -d '=' -f2)"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.49.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}
